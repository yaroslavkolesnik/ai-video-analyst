# Export orders to CSV and mark an order as shipped

In an orders management table.

⛔ **Declined** — no guide was produced from this recording.

**Reason:** `multiple_operations`

---

Built from `E.mp4` (21.1 s), sha256 `dc29a80e0deab1e9…`.

Each step above was checked on its own against two frames cut from that file — one where the control is still visible, one where its effect should be. No step was written from narration alone.

The recording never shows the operation completing, so this guide does not claim it did.

